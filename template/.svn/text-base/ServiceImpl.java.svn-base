package org.ssm.crm520.service.impl;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.ssm.crm520.domain.${domain};
import org.ssm.crm520.mapper.${domain}Mapper;
import org.ssm.crm520.service.I${domain}Service;

@Service
public class ${domain}ServiceImpl extends BaseServiceImpl<${domain}> implements
		I${domain}Service {

	@Autowired
	public void setBaseMapper(${domain}Mapper ${domainLower}Mapper) {
		super.setBaseMapper(${domainLower}Mapper);
	}

}
